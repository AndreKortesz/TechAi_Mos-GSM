# TechBase AI — Mos-GSM AI-ассистент по СКУД и СВН

## О проекте

**TechBase AI** (он же **Mos-GSM AI**) — веб-приложение с AI-ассистентом для менеджеров по продажам компании **Mos-GSM** (комплексные слаботочные системы). Бот помогает менеджерам консультировать клиентов, подбирать оборудование, составлять сметы и коммерческие предложения по СКУД и СВН.

- **Сайт компании:** [mos-gsm.ru](https://mos-gsm.ru)
- **Портал Битрикс24:** svyaz.bitrix24.ru
- **Прод-URL:** https://web-production-65cac.up.railway.app
- **GitHub:** github.com/AndreKortesz/TechAi_Mos-GSM

---

## Для кого это приложение

| Роль | Как использует |
|------|---------------|
| **Менеджер по продажам** | Задаёт вопросы по оборудованию, получает расчёты стоимости, сравнения производителей, помощь в составлении КП |
| **Владелец (Андрей Конторин)** | Администрирование, настройка системы, деплой |

**Важно:** Пользователи — сотрудники компании-интегратора, а НЕ конечные заказчики. Бот никогда не советует «обратиться к интегратору» — потому что менеджер сам работает в интеграторе.

---

## Стек технологий

| Компонент | Технология |
|-----------|-----------|
| Backend | **FastAPI** (Python) |
| AI | **Claude API** (claude-sonnet-4-5-20250929) + web_search tool |
| База данных | **PostgreSQL** (asyncpg) |
| Авторизация | **Битрикс24 OAuth2** |
| Сессии | Starlette SessionMiddleware + itsdangerous |
| HTTP-клиент | httpx (для OAuth) |
| Frontend | Vanilla HTML/CSS/JS (один файл index.html) |
| Деплой | **Railway** (auto-deploy from GitHub) |
| Шрифты | Bebas Neue, Roboto Condensed, JetBrains Mono |

---

## Структура файлов

```
TechAi_Mos-GSM/
├── server.py              # Бэкенд: FastAPI, Claude API, OAuth, PostgreSQL
├── requirements.txt       # Python-зависимости
├── Procfile               # Команда запуска для Railway
├── static/
│   ├── index.html         # Фронтенд: весь UI в одном файле
│   └── favicon.ico        # Иконка
```

---

## Архитектура

```
Браузер (менеджер)
    │
    ├── GET / → если нет сессии → /login → «Войти через Битрикс24»
    │           если есть сессия → static/index.html
    │
    ├── /auth/login → редирект на Битрикс24 OAuth
    ├── /auth/callback → обмен code на token → сессия → /
    ├── /auth/logout → очистка сессии → /login
    ├── /auth/me → JSON с данными пользователя
    │
    ├── POST /api/chat/stream → SSE-стриминг ответа Claude (основной)
    ├── POST /api/chat → обычный ответ (fallback)
    │
    ├── GET /api/sessions → список чатов текущего пользователя
    ├── GET /api/sessions/{id}/messages → сообщения чата
    └── DELETE /api/sessions/{id} → удаление чата
```

### Поток авторизации (OAuth2)

1. Пользователь → `/login` → нажимает «Войти через Битрикс24»
2. Редирект на `svyaz.bitrix24.ru/oauth/authorize/`
3. Битрикс возвращает `code` на `/auth/callback`
4. Сервер обменивает `code` на `access_token` через `oauth.bitrix24.tech`
5. Запрашивает `user.current` — имя, ID, должность, email
6. Сохраняет в серверную сессию (cookie)
7. Редирект на `/` — интерфейс чата

### Поток сообщения

1. Пользователь вводит вопрос → POST `/api/chat/stream`
2. Проверка авторизации (`require_auth`)
3. Загрузка/создание сессии из PostgreSQL (привязана к `user_id`)
4. Сохранение сообщения пользователя в БД
5. Запрос в Claude API: системный промпт + последние 20 сообщений + web_search tool
6. SSE-стриминг ответа в браузер
7. Сохранение полного ответа в БД
8. Фронтенд рендерит markdown

---

## База данных (PostgreSQL)

```sql
chat_sessions (
    id TEXT PRIMARY KEY,           -- UUID[:8]
    user_id TEXT DEFAULT '',       -- Bitrix24 user ID
    user_name TEXT DEFAULT '',     -- Имя сотрудника
    title TEXT DEFAULT '',         -- Первое сообщение (до 80 символов)
    created_at TIMESTAMPTZ,
    updated_at TIMESTAMPTZ
)

chat_messages (
    id SERIAL PRIMARY KEY,
    session_id TEXT → chat_sessions(id) ON DELETE CASCADE,
    role TEXT NOT NULL,            -- 'user' или 'assistant'
    content TEXT NOT NULL,
    created_at TIMESTAMPTZ
)
```

**Изоляция:** каждый пользователь видит только свои чаты (`WHERE user_id = $1`).

---

## Переменные окружения (Railway)

| Переменная | Описание |
|-----------|---------|
| `ANTHROPIC_API_KEY` | Ключ Claude API |
| `DATABASE_URL` | PostgreSQL (ссылка на Railway Postgres) |
| `BITRIX_CLIENT_ID` | ID приложения Битрикс24 |
| `BITRIX_CLIENT_SECRET` | Секрет приложения Битрикс24 |
| `BITRIX_REDIRECT_URI` | `https://web-production-65cac.up.railway.app/auth/callback` |
| `BITRIX_DOMAIN` | `svyaz.bitrix24.ru` (по умолчанию в коде) |

---

## Системный промпт — ключевые правила

### Контекст пользователя
- Менеджеры Mos-GSM — сотрудники интегратора
- Никогда не советовать «обратиться к специалисту»
- Расчёты: оборудование отдельно, монтаж отдельно (для КП клиенту)

### Технические ограничения
- Только текст — НЕ принимает фото/файлы
- Просит описать фото словами

### Экономия веб-поиска
- **Не искать:** настройка, схемы, расчёты, диагностика, сравнения
- **Искать только:** актуальные цены, прямая просьба, редкие модели
- `max_uses: 2` — максимум 2 поиска за запрос
- Без поиска: ~$0.01-0.02/запрос. С поиском: ~$0.10-0.15/запрос

### Области знаний
- **СКУД:** Болид, Sigur, PERCo, RusGuard, Parsec, HID, ZKTeco, Hikvision
- **СВН:** Hikvision, Dahua, Axis, Trassir, Macroscop, IDIS, Uniview
- **Общее:** ONVIF, RS-485, Wiegand, PoE, расчёты архива, монтаж

---

## Фронтенд

### Дизайн
- Жёлтый акцент #D4A53A, чёрный #1A1A1A, белый фон
- Шрифты: Bebas Neue (лого), Roboto Condensed (основной)
- Светлая тема

### Функции
- Сайдбар: список чатов из БД, «+ Новый чат», имя пользователя, выход
- Велком-экран: приветствие по времени суток + 6 случайных вопросов из банка (~195 шт)
- Стриминг ответа, анимации поиска
- Markdown: заголовки, таблицы, код, ссылки
- При 401 → авто-редирект на /login

### Категории вопросов
- 💰 Расчёт стоимости
- 🔄 Сравнение оборудования
- 📋 КП, проекты, нормы
- 🏢 Типовые объекты
- 🤝 Работа с клиентом
- 🔧 Базовая техника

---

## Деплой

1. Railway: подключить GitHub (auto-deploy при push)
2. Добавить PostgreSQL сервис
3. Добавить переменные окружения
4. Зарегистрировать OAuth-приложение в Битрикс24

**Проверка:** в логах должно быть:
```
✅ PostgreSQL подключена, таблицы готовы
✅ User logged in: Имя (ID: X)
```

---

## TODO

- [ ] Pricing tool — прайс-лист в PostgreSQL + tool_use
- [ ] Мобильная версия для монтажников
- [ ] Поддержка фото (multimodal API)
- [ ] Роли: менеджер / монтажник / руководитель
